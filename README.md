# sb2loopback-ng

Using Loopback with Angular SDK.
EventSource for realtime updates.
Offline sync supported.

```
git clone
cd to dir
npm install
bower install
grunt build && node .
http://localhost:3000
```
